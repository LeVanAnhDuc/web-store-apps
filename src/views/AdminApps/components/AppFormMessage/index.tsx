// components
import FormFieldMessage from "@/components/FormFieldMessage";

const AppFormMessage = () => (
  <FormFieldMessage namespace="adminApps.form.validation" />
);

export default AppFormMessage;
